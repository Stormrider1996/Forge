export const resetCompanyFields = (methods: any) => {
  methods.resetField('company')
  methods.resetField('companyAddress')
  methods.resetField('country')
  methods.resetField('city')
  methods.resetField('zipCode')
  methods.resetField('registrationId')
  methods.resetField('vatId')
  methods.resetField('companyEmail')
  methods.resetField('phoneNumber')
}
